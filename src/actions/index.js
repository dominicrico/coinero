import {isLoading} from './fetching'
import {setSettings, getSettings, settingsError} from './settings'

const actions = {
  isLoading,
  setSettings,
  settingsError,
  getSettings
}

export default actions;
